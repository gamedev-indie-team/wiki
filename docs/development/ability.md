## Описание 

- способности подразумевают под собой выполнений широкого спектра эффектов способности
- на текущий момент эффекты могут включать в себя
    - анимация
    - движение
        - мгновенное
        - через физику
    - выполнение/вызов модификаторов
    - создание/вызов из пула объекта
- эффекты способности могут выполнятся
    - после таймера
    - после анимации
    - после таймера и после анимации анимации(порядок неважен)

?> Механизм способности основан на `ECS` и `ООП`

## Способность
 
- для создания новой способности нужно создать `ScriptableObject` с типом `AbilityConfig`
- в `AbilityConfig` можно указать
    - время отката способности
    - список эффектов
- при создании нового эффекта необходимо наследовать от `AbilityEffect`
- для добавление способности можно использовать `AbilityListConverter`

## Эффект способности 

- в `AbilityEffect` можно настроить следующее
    - анимацию (через список)
        - выполнить после анимации, если указана анимация
        - длительность анимации, если указана анимация и нужна длительность отличная от самой анимации
    - использовать таймер
        - таймер

## Анимация

При добавлении новой анимации необходимо сделать следующее
1. в `HumanoidAnimatorController` открыть `AbilityStateMachine`
2. добавить новый `state`
3. добавить условия перехода
4. указать флаг анимации в `AnimatorAbilityState`

## Порядок выполнения

- перед выполнением способности проверяется наличие таймера с куладуном, если есть, то способность не выполнится
- во время выполнения способность "разбирается" на эффекты 
- каждый эффект выполняется по отдельности и в зависимости от настроек, порядок в списке эффектов неважен

## Ссылки

-   [miro - описание AbilityFeature](https://miro.com/app/board/uXjVPrjYGFk=/?moveToWidget=3458764634736809942&cot=14)